#version 330

in vec2 in_pos;
in vec2 in_st;

out vs_output
{
   vec3 color;
   vec2 st;
} v_out;

uniform mat4 mvp;
uniform float time;

void main()
{
   gl_Position = mvp * vec4(in_pos, 0, 1);
   v_out.color = vec3(abs(sin(3 * time)), abs(sin(time + in_pos.x)), 0);
   v_out.st = in_st;
}
